---
sidebar_position: 2
---

# /embed delete

Delete a saved embed from your server’s embed library. Use this to clean up old or unused embeds.

<hr className="md-divider-gradient" />

<h3 className="heading-with-icon">
  <img src="/img/icons/chat.svg" alt="" className="heading-icon" />
  <span>Usage</span>
</h3>
```bash
/embed delete <name>
```
- `<name>` – The name of the embed you want to delete. Must match an existing embed you previously created.

:::success
This action is permanent — once deleted, the embed cannot be recovered unless manually recreated.
:::

<hr className="md-divider-gradient" />

<h3 className="heading-with-icon">
  <img src="/img/icons/example.svg" alt="" className="heading-icon" />
  <span>Example</span>
</h3>
```bash
/embed delete rules
```
This will:
- Permanently delete the embed named rules from your server’s embed storage
- Return a confirmation message once complete

<hr className="md-divider-gradient" />

<h3 className="heading-with-icon">
  <img src="/img/icons/notes.svg" alt="" className="heading-icon" />
  <span>Notes</span>
</h3>
> - Use `/embed list` if you’re unsure what embed names you’ve saved.
> - You cannot delete embeds created by someone else unless you have the proper permissions (depending on how you’ve configured access in your server).

<hr className="md-divider-gradient" />

<h3 className="heading-with-icon">
  <img src="/img/icons/related.svg" alt="" className="heading-icon" />
  <span>Related Commands</span>
</h3>
`/embed create` – Build a new embed from scratch

`/embed edit` – Modify a saved embed

`/embed list` – View all your saved embeds